# Copy this to .env.local and edit when you want to enable real command execution
# Set to the URL of the command server (default server script uses port 4000 on localhost)
VITE_COMMAND_SERVER_URL=http://localhost:4000
# Or set to true to use default local URL
VITE_USE_REAL=true
